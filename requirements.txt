# AutoWhisper Python dependencies

# Core inference - faster-whisper with CUDA support
# Note: Install PyTorch with CUDA first.
# - RTX 50xx (Blackwell, sm_120): use nightly CUDA 12.8 wheels:
#     pip install --upgrade --pre torch torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
# - Older GPUs: stable CUDA wheels are usually fine, e.g.:
#     pip install --upgrade torch torchaudio --index-url https://download.pytorch.org/whl/cu124
faster-whisper>=1.0.0

# Audio
sounddevice>=0.4.6
numpy>=1.24.0

# Hotkeys
pynput>=1.7.6

# X11 output
python-xlib>=0.33

# Configuration
toml>=0.10.2
